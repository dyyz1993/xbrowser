# Gemini

> Google Gemini AI 助手

## 命令

- `list` — 列出历史会话
- `chat` — 发送消息
- `music` — 生成音乐（打开制作音乐工具并发送提示）

## 使用

```bash
xbrowser --cdp 9221 gemini <command> [options]
```

### 登录要求

本插件需要登录才能使用。请先在 Chrome 中登录Gemini，然后通过 `--cdp` 连接。
