set -e

echo "🧪 [pre-push] Running full test suite..."
npx vitest run --exclude='tests/e2e/**' --exclude='tests/human-interaction.test.ts' --exclude='tests/websocket-server.test.ts' --exclude='tests/screencast.test.ts'
if [ $? -ne 0 ]; then
  echo "❌ Tests failed. All tests must pass before pushing."
  exit 1
fi
echo "✅ [pre-push] All tests passed!"
