# DeepSeek 全链路自动化测试需求

## 环境信息

| 项目 | 值 |
|------|-----|
| 目标网站 | https://chat.deepseek.com |
| 浏览器连接 | CDP 端口 9221（已登录的 Chrome 浏览器） |
| 操作系统 | macOS (Darwin) |
| 运行方式 | 通过 CDP 连接现有浏览器，不启动新浏览器实例 |

## 测试目标

在 DeepSeek 网站上执行以下全链路操作，所有操作均通过 CDP 连接已登录的浏览器完成。

## 详细需求步骤

### 1. 连接并查看会话列表
- 通过 CDP `http://localhost:9221` 连接到用户浏览器
- 打开 DeepSeek 首页 `https://chat.deepseek.com/`
- 获取并展示左侧侧栏中的所有历史会话标题列表
- **注意：** 不要新建页面（`newPage`），否则看不到历史会话

### 2. 点击/选择一个会话
- 从会话列表中点击任意一个历史会话
- 验证页面导航到了具体的对话页面（URL 模式：`/a/chat/s/{uuid}`）

### 3. 切换模式
- **专家/普通模式切换：**
  - 可选项：快速模式、专家模式
  - 点击切换并验证选中状态变化
  - **注意：** 模式切换控件仅在首页显示，进入具体会话后隐藏
- **深度思考开关：**
  - 点击"深度思考"按钮
  - 验证开启/关闭状态（`[pressed]` 属性变化）
  - 来回切换至少一次
- **智能搜索开关：**
  - 点击"智能搜索"按钮
  - 验证开启/关闭状态
  - 来回切换至少一次

### 4. 新建对话
- 在侧栏中找到"新聊天"或"开启新对话"按钮
- 点击创建新对话
- 验证页面状态变为新的空白对话

### 5. 发送消息
- 在输入框中输入一条消息（如 "Hello, what is AI?"）
- 通过 Enter 或点击发送按钮提交
- 等待并验证 AI 返回了响应内容

## 成功标准

所有步骤应顺序执行成功，每个步骤有明确的成功/失败反馈。

## 已知难点（踩坑参考）

1. DeepSeek 的图标按钮没有 aria-label 和 title，需通过 DOM 结构定位
2. 新聊天按钮在侧栏顶部，是纯图标 `div[role="button"]`，无文字标识
3. 会话列表只有在已有页面中才可见，新页面不会自动加载历史
4. 类名被 CSS Modules 混淆，不能用 className 做稳定选择器
5. 模式切换（快速/专家）只出现在首页，进入具体对话后消失

## 输出格式

请记录：
- 每个步骤的执行耗时（秒）
- 成功/失败状态
- 失败时给出原因和错误信息
- 总体耗时
